---
id          = "extra-sum-10-args"
name        = "Function Sum 10 Args"
language    = "riscv"
difficulty  = 3
description = "Implement a 10-argument sum function following RV32 calling convention."
topics      = ["functions", "stack", "arguments", "abi"]
---

# Function Sum 10 Args

## Tasks

Implement:

```c
res = sum(a,b,c,d,e,f,g,h,i,j);
```

with values `1..10`, and store result in `s11`.
