# Theory

This exercise implements multiplication without `mul`.

## Core Concepts

- `x * y` can be computed by adding `x` repeatedly `y` times.
- A loop counter controls how many additions are performed.
- Branch instructions terminate when the counter reaches zero.
