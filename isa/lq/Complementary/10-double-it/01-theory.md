# Theory

This exercise focuses on function calls and register preservation.

## Core Concepts

- Pass argument in `a0` and return value in `a0`.
- Save and restore `s` registers when used inside a function.
- A leaf optimized variant can compute directly in `a0`.
