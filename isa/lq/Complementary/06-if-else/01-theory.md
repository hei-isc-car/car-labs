# Theory

This exercise maps `if / else if / else` logic to branches.

## Core Concepts

- Use one compare and branch per decision point.
- Separate each path with labels.
- Jump to a common `end` label when a result has been selected.
