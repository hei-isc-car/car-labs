---
id          = "extra-if-else"
name        = "If Else"
language    = "riscv"
difficulty  = 2
description = "Implement an if/else-if/else decision block in assembly."
topics      = ["branching", "if-else"]
---

# If Else

## Tasks

Implement this logic:

```c
int a = 1, b = 2, c;
if (a == b) {
  c = 0;
} else if (a > b) {
  c = 1;
} else {
  c = 2;
}
```

Store the result in `s2`.
