# Theory

This exercise practices serial bit transmission with memory-mapped I/O.

## Core Concepts

- Read a byte from memory with `lb`.
- Emit control and data bits with `sb`.
- Use a mask and shifts to send bits from least significant bit to most significant bit.
